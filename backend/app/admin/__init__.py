"""Admin API (see PRD FR-ADMIN).

TODO: implement
- Users CRUD, role assignments, hub scope assignments (all role=admin only).
- Config KV CRUD (spreadsheet ID, column mapping, comments, benchmark notes,
  city pairs, retention windows).
- Every mutation writes an audit log entry in the same transaction.
"""
